# Docker final task

## instaruction

1. clone repo
   ````
   ```
   git clone git@github.com:ngga-wt/docker_final_task.git
   cd docker_final_task
   ```
   ````
2. make executable to run-docker.sh file
   ```

   ```
   ````
   ```
   sudo chmod +x run-docker.sh
   ```
   ````
3. execute run-docker.sh file
   ```

   ```
   ````
   ```
   ./run-docker.sh
   ```
   ````
